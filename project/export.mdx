---
title: '导出'
description: '多种导出方式，多种导出格式'
---

## IP端口数据

点击菜单栏的 `端口服务`，进入页面后点击`导出`按钮，选择格式即可导出

## 子域名网站数据

点击菜单栏的 `网站列表`，进入页面后点击`导出`按钮，选择格式即可导出

## 漏洞导出

点击右上角的 `应用面板`，进入 `项目漏洞导出` 应用后点击 `执行`，然后选择导出